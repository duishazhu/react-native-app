import { createStyle } from 'styles';

export default createStyle({
  page: {
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    height: '100vh',
    backgroundColor: '#FFE423',
  },
  logo: {
    width: 107,
    height: 40,
  },
});
