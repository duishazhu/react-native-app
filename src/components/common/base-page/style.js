import { createStyle } from 'styles';

export const orderDetailStyles = createStyle({
  header: {
    zIndex: 2,
    backgroundColor: 'transparent',
  },
  background: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: 268,
    background: 'linear-gradient(180deg, rgba(255, 228, 35, 1) 0%, rgba(255, 228, 35, 0) 100%)',
  },
});
