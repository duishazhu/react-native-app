import { createStyle } from 'styles/theme';

export default createStyle({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },

  text: {
    marginLeft: 4,
    fontSize: 12,
  },
});
