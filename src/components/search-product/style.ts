import { createStyle } from 'styles';

export default createStyle({
  goodItemWrapper: {
    backgroundColor: '$white',
    height: 130,
    paddingVertical: 10,
    paddingLeft: 16,
    paddingRight: 20,
  },
});
