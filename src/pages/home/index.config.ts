import { IPageConfig } from '@terminus/octopus-shared';

const config: IPageConfig = {
  disableScroll: true,
  usingComponents: {
    'native-carousel-0': '/vendors/npm/terminus/nusi-mobile/lib/carousel/native-carousel/index',
  },
};

export default config;
